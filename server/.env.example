PORT=4000
NODE_ENV=development
MONGODB_URI=mongodb://localhost:27017/niraj

# Owner email
OWNER_EMAIL=bombaylab.vishwakarma@gmail.com

# SMTP (Gmail App Password recommended)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=your.gmail@gmail.com
SMTP_PASS=your_app_password

# Acknowledgment email to submitter (true/false)
SEND_ACK=false

# Admin
JWT_SECRET=change_this_to_a_long_random_value
ADMIN_USERNAME=admin
# bcrypt hash for the admin password (generate with: node -e "console.log(require('bcryptjs').hashSync('YourPassword',10))")
ADMIN_PASSWORD_HASH=$2a$10$replace_with_bcrypt_hash

# CORS
CORS_ORIGIN=http://localhost:5173
